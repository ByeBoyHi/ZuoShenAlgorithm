Manacher:
1. 回文半径：回文串的一半，称为半径（整个长度成为回文直径）
2. 最右侧R：目前能到达的回文串的最右侧的位置
3. 当前中心位置C：取得更远边界R的时候，中心点的位置，R更新，C就跟着更新
4. 从左往右把每个位置的回文半径记录下来，得到回文半径数组

第一种情况：
当来到某一个中心点的时候，这个点没有在回文最右边界里面，这时候暴力扩。
第二种情况：
如果在回文最右边界里面，
1. 如果对称点的回文半径在R的对称点L的里面，那么他俩回文半径是一样的。
2. 如果对称点的回文半径超过了L，那么当前点的回文半径就是到R就结束的距离。
3. 如果对成电的回文半径压线了，刚好在L，那么当前点的回文半径就需要从边界往外验证。
C R就是为这个服务的。
